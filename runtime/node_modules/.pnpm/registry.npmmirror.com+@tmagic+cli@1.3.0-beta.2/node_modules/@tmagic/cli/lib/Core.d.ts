import { ModuleMainFilePath, UserConfig } from './types';
export default class Core {
    version: any;
    options: UserConfig;
    moduleMainFilePath: ModuleMainFilePath;
    dir: {
        temp: () => string;
    };
    constructor(options: UserConfig);
    writeTemp(file: string, content: string): Promise<void>;
    init(): Promise<void>;
    prepare(): Promise<void>;
}
